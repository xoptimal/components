import type { PopupsProps, PopupsRef, PopupType } from './Popups';
import Popups from './Popups';
import usePopup from './hooks/usePopup';

export type {
  PopupsRef, PopupsProps, PopupType,
};

export {
  usePopup,
};

export default Popups;
